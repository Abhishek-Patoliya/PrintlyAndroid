package com.a8000053398.printly.viewmodel

/** A sheet to open automatically the first time the editor appears — used by
 * Home's "Custom Size" and "Labels & Stickers" quick actions. */
enum class EditorInitialAction { PHOTOS, CUSTOM_PHOTO_SIZE, ADD_LABEL }
