<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Covers filesDir/exports (ExportHistoryStore's kept copies) -->
    <files-path name="internal_files" path="." />
    <!-- Covers cacheDir/tmp (freshly generated PDFs/images before sharing/printing)
         and cacheDir/camera (temporary camera capture output) -->
    <cache-path name="cache" path="." />
</paths>
